import http = require('http');
/*
const card = document.getElementById("card")
const loadMoreButton = document.getElementById("ViewMoreButton")
*/

class MainPage {
    static displayMentorImages {
        xhttp.open("GET", "/api/main_page", true);
    }
}